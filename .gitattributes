# Normalize line endings so shell scripts run on Linux/macOS regardless of cloner's OS.
* text=auto
*.sh              text eol=lf
hooks/pre-commit  text eol=lf
*.ps1             text eol=crlf
*.md              text
