# APPCORE — code map (re-synced <date>, base <version>)

> What lives where. 1–3 lines per entry. A map, not documentation.
> If it wasn't verified with grep or a read, it doesn't get written here.

# DIRECTORIES
- /<dir>/: <one line>

# ENTRYPOINTS / ENDPOINTS (with auth gate in parentheses)
- <route/command> (<gate>): <what it does>

# PROTOCOLS (message shapes, events, contracts)
- <...>

# CRITICAL FILES (1–3 lines each)
- <path>: <role, key functions, gotchas>

# EXTERNAL / DEPLOY
- <services, build, release pipeline>

<!-- reindex: after every release or when this index misleads you, verify against the
     REAL repo (grep endpoints/routes/protocols), fix stale, add new, delete dead.
     Never write unverified. Note "APPCORE re-synced <date> (N changes)" in CURRENT.md. -->
